<import resource="classpath:alfresco/module/alfviral/templates/scripts/ScanFolder.js">

/*
 * Escanea a partir del espacio de forma recursiva
 */
logger.log("ScriptScanFolder");

if (space)
{
    var f = space.displayPath + "/" + space.name;
    var scanfolder = ScanFolder(f, "r");
}
else
{
    logger.log("Error al intentar acceder al espacio.");
}

